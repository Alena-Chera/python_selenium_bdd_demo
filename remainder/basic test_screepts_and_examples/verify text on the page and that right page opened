


@then('verify Pre_registration page {page_name} opened and user can see a text {text}')
def verify_link_sent_page_opened_user_can_sees_text_about_it(context, page_name, text):
    context.driver.wait.until(EC.presence_of_element_located(TEXT_VERIFY))
    text_verify = context.driver.find_element(*TEXT_VERIFY).text
    print(text_verify)
        assert f'{text}' in text_verify, f'Expected "{text}", but got "{text_verify}"'
    
    
    expected_title = "Lyte — Pre-registration"
    current_title = context.driver.title
    assert expected_title in current_title, f'url is wrong. Expected title is "{expected_title}", but actual url is "{current_title}"'

